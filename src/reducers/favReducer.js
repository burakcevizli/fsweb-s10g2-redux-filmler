const initialState = {
    favorites: {
        film: []
    },
    displayFavorites: false,
}

export const favReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}
