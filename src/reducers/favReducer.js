const initialState = {
    favorites: {
        Film: []
    },
    diplayFavorites: false,
}

export const favReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}
